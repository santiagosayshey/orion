http:
  routers:
    plex:
      rule: "Host(`plex.orion`)"
      entryPoints:
        - web
      service: plex

  services:
    plex:
      loadBalancer:
        servers:
          - url: "http://192.168.8.239:32400"
